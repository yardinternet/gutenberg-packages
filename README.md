# Gutenberg Packages

## Linting and formatting

This repos has builtin support for eslint and prettier. To enable formatting make sure the following setting is present

```JSON
   "editor.formatOnSave": true
```
