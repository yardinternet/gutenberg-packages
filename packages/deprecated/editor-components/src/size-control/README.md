# Sizecontrol

Implements a size control to set the height of a component.

Example
```
<SizeControl { ...{ setAttributes } } value={ minHeight } onChange={ ( val ) => setAttributes( { minHeight: val } ) } label="Min-height" />
```
